# Update matrix based on events list from createNameEvents

Update matrix based on events list from createNameEvents

## Usage

``` r
processNameEvents(m, events, row, col)
```

## Arguments

- m:

  matrix

- events:

  event list from createNameEvents

- row:

  variable used for row names

- col:

  variable used for column names

## Value

altered matrix
