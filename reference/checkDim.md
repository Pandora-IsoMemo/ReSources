# Check if dimensions are correct

Check if dimensions are correct

## Usage

``` r
checkDim(x, dimExpected, argName = NULL)
```

## Arguments

- x:

  any object

- dimExpected:

  numeric: vector of expected dimensions

- argName:

  character: name of argument displayed in error message
