# Target Values Server

Server function of the data - target values

## Usage

``` r
targetValuesServer(id, values, events, termChoices, modelType)
```

## Arguments

- id:

  id of module

- values:

  values

- events:

  events

- termChoices:

  termChoices

- modelType:

  (reactive) character id, e.g. "1"
