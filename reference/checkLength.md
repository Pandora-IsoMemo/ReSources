# Check if length is correct

Check if length is correct

## Usage

``` r
checkLength(x, lengthExpected, argName = NULL)
```

## Arguments

- x:

  any object

- lengthExpected:

  numeric or integer of length 1: expected length

- argName:

  character: name of argument displayed in error message
