# Extract contribution limit

Extract contribution limit

## Usage

``` r
extractContributionLimit(estType, userEstimateGroups)
```

## Arguments

- estType:

  (character) type of estimates, e.g. "Source contributions", "Component
  contributions", ...

- userEstimateGroups:

  (list) list of user estimate groups
